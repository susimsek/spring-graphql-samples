query GetAllPosts($page: Int, $size: Int, $orders: [PostOrder!]) {
    posts(page: $page, size: $size, orders: $orders){
        id,
        title,
        content,
        status,
        createdDate
    }
}