query SearchPosts($page: Int, $size: Int, $searchPhrase: String!) {
    searchPosts(page: $page, size: $size, searchPhrase: $searchPhrase) {
        id
        title
        content
        status
        createdAt
    }
}