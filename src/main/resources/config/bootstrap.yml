spring:
  application:
    name: spring-gql-service
  profiles:
    active: local
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 5000
      config:
        sources:
          - name: ${spring.application.name}-configmap
---
spring:
  config:
    activate:
      on-profile: local
  cloud:
    kubernetes:
      enabled: false